
require:
   "./util" ->
      ID, methods
   "./proxy" ->
      deprox

provide:
   iterate-properties


iterate-properties{match obj} =
   null? or undefined? ->
      {}
   else ->
      deprox{obj}[methods.iterate-properties]{}

